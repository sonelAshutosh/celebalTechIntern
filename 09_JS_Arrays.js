// JS Arrays

var arr = [1, 2, 3, 4, 5]
console.log(arr)
console.log('Element at arr[0]: ' + arr[0])

var strArr = ['One', 'Two', 'Three', 'Four']
console.log(strArr)

strArr.push('Five')
console.log(strArr)

let unSorted = [9, 7, 3, 5, 1, 4, 2]
console.log(unSorted.sort())

// Iterate over an Array
for (let i = 0; i < strArr.length; i++) {
  console.log(strArr[i])
}
